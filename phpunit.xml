<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
        <testsuite name="Browser">
            <directory>tests/Browser</directory>
        </testsuite>
        <testsuite name="Components">
            <directory suffix=".test.php">resources/views</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!-- Der PHP-Default von 128M reicht fuer den vollen Lauf nicht mehr: am
             2026-08-23 brach er im letzten Browser-Test mit
             "Allowed memory size of 134217728 bytes exhausted" ab, obwohl derselbe
             Test isoliert gruen war. Der Speicher wird ueber ~960 Tests hinweg
             nicht vollstaendig freigegeben; 1G ist die Grenze, nicht der Bedarf. -->
        <ini name="memory_limit" value="1G"/>
        <env name="APP_ENV" value="testing"/>
        <!-- Das Aenderungs-Log (Issue #29) ist im Test per Default AUS. Sonst loeste
             jeder Fixture-Aufbau eine JsonResource-Aufloesung plus zwei Schreibvorgaenge
             aus — bei ueber 250 Datensaetzen allein aus dem DatabaseSeeder. Die Tests,
             die das Log pruefen, schalten es selbst per config()->set() an. -->
        <env name="CHANGE_LOG_ENABLED" value="false"/>
        <!-- Ohne diese Zeile faellt der Broadcast-Treiber auf das .env des Entwicklers
             zurueck. Steht dort seit P4 BROADCAST_CONNECTION=reverb, versucht jeder
             Test, der eine Ressource anlegt, einen echten HTTP-Call an
             localhost:8080 — und die halbe Suite wird rot, sobald der Daemon nicht
             laeuft. Ein Test darf nie von einem Dienst abhaengen, den er nicht
             gestartet hat. Die Broadcast-Tests setzen sich ihren eigenen Treiber. -->
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="DB_FOREIGN_KEYS" value="true"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="APP_LOCALE" value="de"/>
        <env name="CIPHERSWEET_KEY" value="45186542ebd89a8c7d719543be7531c40a795c40582ceddfca758189d83ce356"/>
        <env name="PERMISSION_TESTING" value="true"/>
        <!-- Hier stand PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=/usr/bin/chromium. Die Variable
             existiert in playwright-core nicht (grep über coreBundle.js: 0 Treffer) und war
             wirkungslos — sie sicherte zu, was nicht passierte: die Browser-Tests liefen
             gegen Playwrights eigenen Build (HeadlessChrome/149.0.7827.55).
             Die wirksame Verdrahtung steht jetzt in tests/Pest.php und läuft über
             PLAYWRIGHT_BROWSERS_PATH; gemessen meldet navigator.userAgent seither
             HeadlessChrome/151.0.0.0 — die Version des System-Chromium. -->
        <env name="PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD" value="1"/>
    </php>
</phpunit>
