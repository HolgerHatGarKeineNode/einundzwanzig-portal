name: laravel-boost
version: 0.0.1
schema: v1
mcpServers:
  - name: laravel-boost
    command: docker
    args:
      - exec
      - -i
      - einundzwanzig-app-laravel.test-1
      - php
      - /var/www/html/artisan
      - boost:mcp
    env: {}
